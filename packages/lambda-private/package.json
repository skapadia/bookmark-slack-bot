{
  "name": "@bookmark-slack-bot/lambda-private",
  "version": "1.0.0",
  "description": "Private Lambda function for database operations",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo",
    "dev": "tsc --build --watch",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --max-warnings 10"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.490.0",
    "@bookmark-slack-bot/shared": "*",
    "@bookmark-slack-bot/core": "*",
    "@bookmark-slack-bot/api-contracts": "*",
    "@aws-sdk/client-secrets-manager": "^3.490.0",
    "pg": "^8.11.0",
    "fuzzball": "^2.1.2",
    "wink-lemmatizer": "^3.0.4"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.10.0",
    "@types/aws-lambda": "^8.10.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}